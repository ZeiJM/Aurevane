# Balance Content

Versionable balance inputs, tuning tables, simulation fixtures, and related data will live here when those systems are implemented.

Balance values must be consumed by authoritative server/game-rule code. Do not place authoritative tuning exclusively in client components.
