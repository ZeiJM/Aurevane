name: Format battle log branch

on:
  push:
    branches:
      - agent/battle-log-narrative-polish

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/battle-log-narrative-polish
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.17.0 --activate
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Format battle log files
        run: |
          pnpm exec prettier --write \
            apps/web/src/components/battle/battle-log-presentation.ts \
            apps/web/src/components/battle/battle-log-presentation.test.ts
      - name: Commit formatting
        run: |
          git config user.name "AUREVANE Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add \
            apps/web/src/components/battle/battle-log-presentation.ts \
            apps/web/src/components/battle/battle-log-presentation.test.ts
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore: format battle log presentation"
          git push origin HEAD:agent/battle-log-narrative-polish
