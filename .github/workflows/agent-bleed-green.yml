name: Verify Bleed GREEN

on:
  push:
    branches:
      - agent/combat-overhaul-task4-bleed
    paths:
      - .github/workflows/agent-bleed-green.yml

permissions:
  contents: write

jobs:
  verify-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact isolated revision
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.17.0 --activate
      - name: Install pinned dependencies
        run: pnpm install --frozen-lockfile
      - name: Apply current Bleed runtime
        run: |
          python .github/agent-bleed-green.py
          python .github/agent-bleed-web-fix.py
      - name: Normalize changed source formatting
        run: |
          pnpm exec prettier --write \
            packages/game-core/src/combat/actions.ts \
            packages/game-core/src/combat/combat-dots.ts \
            packages/game-core/src/combat/combat-authoring-validation.ts \
            packages/game-core/src/combat/pv1f-action-economy.ts \
            packages/game-core/src/combat/combat-bleed-rework.test.ts \
            apps/web/src/components/character/skill-detail-presentation.ts
      - name: Run focused Bleed contract
        run: pnpm --filter @aurevane/game-core exec vitest run src/combat/combat-bleed-rework.test.ts
      - name: Run repository quality gates
        run: pnpm check
      - name: Verify patch cleanliness
        run: git diff --check
      - name: Remove temporary verifier files
        run: |
          rm .github/agent-bleed-green.py
          rm .github/agent-bleed-web-fix.py
          rm .github/workflows/agent-bleed-red.yml
          rm .github/workflows/agent-bleed-green.yml
      - name: Commit verified source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add packages/game-core/src/combat/actions.ts \
            packages/game-core/src/combat/combat-dots.ts \
            packages/game-core/src/combat/combat-authoring-validation.ts \
            packages/game-core/src/combat/pv1f-action-economy.ts \
            packages/game-core/src/combat/combat-bleed-rework.test.ts \
            apps/web/src/components/character/skill-detail-presentation.ts \
            .github/agent-bleed-green.py \
            .github/agent-bleed-web-fix.py \
            .github/workflows/agent-bleed-red.yml \
            .github/workflows/agent-bleed-green.yml
          git commit -m 'feat: implement current bleed stacks'
          git push origin HEAD:agent/combat-overhaul-task4-bleed
